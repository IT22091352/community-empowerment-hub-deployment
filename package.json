{
  "name": "community-empowerment-hub",
  "version": "1.0.0",
  "description": "Monorepo for client and server",
  "scripts": {
    "start": "npm --prefix server start",
    "heroku-postbuild": "npm --prefix server install && npm --prefix client install --legacy-peer-deps && npm --prefix client run build",
    "dev": "npm --prefix server run dev"
  },    
  "engines": {
    "node": "18.x"
  }
}